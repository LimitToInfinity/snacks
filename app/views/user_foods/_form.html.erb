<%= form_with(model: user_food, local: true) do |form| %>
  <% if user_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_food.errors.count, "error") %> prohibited this user_food from being saved:</h2>

      <ul>
      <% user_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :User %>
    <%= form.text_field :User %>
  </div>

  <div class="field">
    <%= form.label :Food %>
    <%= form.text_field :Food %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
